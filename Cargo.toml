[package]
name = "gtk-rs-msvc"
version = "0.1.0"
authors = ["kykc"]
build = "build.rs"

[dependencies]

[target.'cfg(windows)'.build-dependencies]
vcpkg = "0.2.6"

[dependencies.gtk]
version = "0.5.0"
features = ["v3_20"]
